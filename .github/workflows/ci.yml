name: ci

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name : Build
        shell: bash
        run : cmake . && make
      - name : Test
        run: ctest
